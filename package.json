{
    "name": "linkify-video",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd client && npm install --include=dev && npm run build",
        "install-server": "cd server && npm install",
        "vercel-build": "npm run build && npm run install-server"
    },
    "workspaces": [
        "client",
        "server"
    ],
    "engines": {
        "node": "20.x"
    }
}