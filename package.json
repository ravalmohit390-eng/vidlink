{
    "name": "linkify-video",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "npm install --legacy-peer-deps && cd client && npm run build",
        "vercel-build": "npm run build",
        "start": "node server/index.js"
    },
    "workspaces": [
        "client",
        "server"
    ],
    "engines": {
        "node": "20.x"
    }
}